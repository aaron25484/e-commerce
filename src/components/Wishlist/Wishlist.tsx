import { FC } from "react"


const WishlistPage: FC = () => {

    const wishlistItem = 1
    return (
        <div>
            <h5>Wishlist</h5>
            { wishlistItem && (

            )

            }
        </div>
    )

}